{
  "name": "@reineira-os/x402-contracts",
  "version": "0.0.0",
  "description": "Deployable RSS implementations — Delivery resolver/policy and X402 escrow receiver",
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "compile": "forge build",
    "test": "forge test -vv",
    "clean": "forge clean",
    "lint": "forge fmt --check",
    "format": "forge fmt"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "@reineira-os/rss": "workspace:*",
    "@fhenixprotocol/cofhe-contracts": "0.1.3",
    "@cofhe/foundry-plugin": "^0.5.2",
    "@cofhe/mock-contracts": "^0.5.2"
  }
}
