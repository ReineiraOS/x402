{
  "name": "@reineira-os/rss",
  "version": "0.0.0",
  "description": "Reineira Settlement Standard — plaintext IConditionResolver / IUnderwriterPolicy conformance suites, the interface-only IFundingSource, and the confidential (FHE) profile: IConfidentialFundingSource / IConfidentialUnderwriterPolicy / IConfidentialConditionResolver with conformance suites",
  "license": "MIT",
  "files": [
    "contracts"
  ],
  "scripts": {
    "build": "forge build",
    "compile": "forge build",
    "test": "forge test -vv",
    "clean": "forge clean",
    "lint": "forge fmt --check",
    "format": "forge fmt"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "@fhenixprotocol/cofhe-contracts": "0.1.3",
    "@cofhe/foundry-plugin": "^0.5.2",
    "@cofhe/mock-contracts": "^0.5.2"
  }
}
