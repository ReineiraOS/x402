{
  "name": "@reineira-os/rss",
  "version": "0.0.0",
  "description": "Reineira Settlement Standard — IConditionResolver / IUnderwriterPolicy / IFundingSource semantics and conformance suite",
  "license": "MIT",
  "files": ["contracts"],
  "scripts": {
    "build": "forge build",
    "compile": "forge build",
    "test": "forge test -vv",
    "clean": "forge clean",
    "lint": "forge fmt --check",
    "format": "forge fmt"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
